language: python

os: linux

python:
  - "2.7"
  - "3.7"

cache:
  - pip

before_script:
  - echo $TARGET

jobs:
  include:
    - stage: test-eg
      env: TARGET=test-target
      script: python test_calc.py
    - stage: deploy-eg
      env: TARGET=deploy-target
      script: python deploy.py
  
notifications:
    email: false