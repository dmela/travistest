language: python

os: linux

cache:
  - pip
  - directories:
    - /home/travis/build/dmela/travistest/hugefiles/

jobs:
  include:
  - python: "2.7"
    env: PYTHON_VERSION=py27
  - python: "3.7"
    dist: bionic
    env: PYTHON_VERSION=py37

before_script:
  - echo $PYTHON_VERSION
  - pwd
  - echo $HOME
  - ls -al
  - ls -al $HOME

script:
  - python test_calc.py

notifications:
    email: false