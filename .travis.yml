language: python

os: linux

cache:
  - pip

jobs:
  include:
    - stage: test-eg
      env: TARGET=test-target
      python:
        - "2.7"
        - "3.7"
      script: python test_calc.py
    - stage: deploy-eg
      env: TARGET=deploy-target
      python: "3.7"
      script: python deploy.py

after_script:
  - echo $TARGET

notifications:
    email: false