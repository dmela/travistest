dist: xenial

services: docker

language: python

env:
  global:
    - PY_VERSION="!!--NO PYTHON VERSION DEFINED--!!"
    - TEST_SUITE=test_calc.py

jobs:
  include:
    # Python-version-tests has three (3) parallel jobs mainly.
    # Maily because of we want to test the compatibility of the app
    # with three different python versions.
    - stage: python-version-tests
      python: "2.7"
      env:
        - PY_VERSION=27
    - # parallel job in python-version-tests stage
      python: "3.7"
      env:
        - PY_VERSION=37
          HELLO="Hello from python version 3.7 build"
    - # parallel job in python-version-tests stage
      python: "3.8"
      env: 
        - PY_VERSION=38
    - stage: container-tests
      language: bash
      before_install:
        - docker pull python:3.6.9-alpine3.9
        - docker build -t my-calc .
      script:
        - docker run --rm my-calc python $TEST_SUITE
      after_script:
        - echo $DOCKER_USERNAME # This is defined in web
        - echo $DOCKER_PASSWORD # This is defined in web
        # - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
        # - docker push $DOCKER_USERNAME/my-calc-travis-docker-sandbox
    - stage: deploy
      python: "3.7"
      env: 
        - PY_VERSION=37
          TEST_SUITE=deploy.py

before_script:
  - echo $PY_VERSION
  - echo $TRAVIS_PYTHON_VERSION
  - if [ $PY_VERSION == "37" ]; then
      echo $HELLO;
    fi

script: python $TEST_SUITE

stages:
  # stages are executed in the following order
  - python-version-tests
  - container-tests
  - name: deploy # run deploy build only in master branch
    if: branch = master

notifications:
    email: false