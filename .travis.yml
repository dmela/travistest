
language: python

python:
  - "2.7"
  - "3.7"

env:
  - FOO=foo BAR=bar
  - FOO=bar BAR=foo
  - FOO=asd BAR=dsa

jobs:
  exclude:
    - python: "3.7"
    - env: FOO=foo BAR=bar

before_script:
  - echo $FOO
  - echo $BAR

script:
  - python test_calc.py

notifications:
    email: false